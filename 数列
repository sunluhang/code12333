An{
A1=0;A2=1;A3=1;
An=An-1+2An-2+An-3;




#include <stdio.h>

int main() {
    int n;
    scanf("%d", &n);
    
    long long a[21] = {0};  // 防止溢出，用 long long
    a[1] = 0;
    a[2] = 1;
    a[3] = 1;
    
    for (int i = 4; i <= n; i++) {
        a[i] = a[i-3] + 2*a[i-2] + a[i-1];
    }
    
    printf("%lld", a[n]);
    
    return 0;
}



用long long 是因为怕A（20）过大
