数组的集成初始化：
int a[]={2,4,6,7,1,3,5,9,11,13,23,14,32};
直接用大括号给出数组的所有元素的初始值
不需要给出数组的大小，编译器替数；


定位：
int a[10]={
[0]=2,[2]=3,6,};
没有定位的数据接在前面的位置后面；
其他位置的值用零补；



数组的大小：
sizeod 给出整个数组所占据的内容的大小，单位是字节(整数是4)
sizeof(a)/sizeof(a[0])
sizeof(a[0])给出数组中单个元素的大小，于是相除就能得到数组的单元个数；



数组的赋值：
int a[]={2,4,6,5}；
int b[]=a是错误的；
数组的变量本身不能被赋值；
要把一个数组的所有元素交给另一个数组，必须采用遍历；
for(i=0;i<length;i++){
b[i]=a[i];}




遍历数组：
for(i=0;i<length;i++){
b[i]=a[i];}//做赋值；
for(i=0;i<number;i++){
count[i]=0;}//初始化；
for(i=0;i<length;i++){
if(a[i]==key){
ret=i;
break;
}
}//判断要找的东西是否存在；
for(i=0;i<cnt;i++){
if(number[i]>average){
printf("%d",number[i]);
}}//看结果要不要输出；
for(i=0;i<number;i++){
printf("%d:%d\n",i,count[i]);
}//遍历看每一个值的大小；

构造素数表：
#include <stdio.h>
int main(){
const int maxNumber =25;
int isPrime[maxNumber];
int i;
int x;
for(i=0;i<maxNumber;i++){
isPrime[i]=1;
}
for(x=2;x<maxNumber;x++){
if(isPrime[x]){
for(i=2;i*x<maxNumber;i++){
isPrime[i*x]=0;}
}
}
for(i=2;i<maxNumber;i++){
if(isPrime[i]){
printf("%d\t",i);
}
}
return 0;}
printf("\n");
//for(i=0;i<maxNumber;i++){
isPrime[i]=1;
}初始化都为素数；









二维数组：
遍历：
for(i=0;i<3;i++){
    for(j=0;j<5;j++){
    a[i][j]=i*j;
    }
}
a[i][j]表示第i行第j列上的单元；



初始化：
int a[][5]={
   {0,1,2,3,4},
   {2,3,4,5,6},
   };
列数必须给出，行数可以由编译器来数；
每行一个{}，逗号分隔；
如果省略表示补零；
